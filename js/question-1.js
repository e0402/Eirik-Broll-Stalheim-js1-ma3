//Question 1
function getRemainder(a, b) {
  return a % b;
}
//Converting the above function to an arrow function

const getRemainder = (a, b) => a % b;

//To call the arrow function we can write it like this

const result = getRemainder(a, b);

console.log(result);
